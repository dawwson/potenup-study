package org.ohgiraffers.junit.chap02;

/*
 * ============================================
 * section01: 좋은 TDD를 위한 FIRST 원칙
 * ============================================
 * [수업용]
 * 🧩 '좋은 단위 테스트'가 가져야 할 5가지 속성 (FIRST)
 * - TDD 사이클(Red-Green-Refactor)을 원활하게 유지하기 위해 테스트 코드는
 * 'FIRST' 원칙을 따라야 합니다.
 */

/*
 * ✅ [F]ast (빠르게)
 * - 정의: 테스트는 '즉각적인 피드백'을 줄 수 있도록 매우 빠르게 실행되어야 합니다. (수백 개의 테스트가 몇 초 내에)
 * - 속성: 느린 테스트(DB 연결, 파일 I/O, 네트워크 호출)는 TDD 사이클을 방해합니다.
 * - 실천: 이것이 Module 04에서 `Mock`을 사용해 외부 의존성을 차단한 이유입니다.
 */

/*
 * ✅ [I]solated / [I]ndependent (고립된 / 독립적인)
 * - 정의: 각 테스트는 서로 완벽하게 독립적이어야 하며, 순서에 관계없이 실행 가능해야 합니다.
 * - 속성:
 * - (고립 1) 다른 테스트의 결과에 영향을 받으면 안 됩니다. (예: `test1`이 실패하면 `test2`도 실패)
 * - (고립 2) 외부 환경(DB, 파일, 시간)에 의존하면 안 됩니다.
 * - 실천: `Module 02`의 `@BeforeEach`는 각 테스트의 독립성을 보장하기 위해 사용됩니다.
 */

/*
 * ✅ [R]epeatable (반복가능한)
 * - 정의: 테스트는 어떤 환경(내 PC, 동료 PC, 빌드 서버)에서도, 몇 번을 실행하든 항상 동일한 결과를 반환해야 합니다.
 * - 속성: 외부 환경(예: DB 데이터 상태, 현재 시간, OS)에 따라 결과가 바뀌면(Flaky Test) 안 됩니다.
 * - 실천: `Mock` 객체를 사용해 외부 환경을 '고정된 값'(Stub)으로 제어합니다.
 */

/*
 * ✅ [S]elf-Validating (자가-검증)
 * - 정의: 테스트는 실행 결과(성공/실패)를 스스로 명확하게 반환해야 합니다.
 * - 속성: 테스트 실행 후, 개발자가 콘솔 로그(e.g., `System.out.println`)를 수동으로 확인해야 한다면 자가-검증이 아닙니다.
 * - 실천: `assertEquals`, `assertTrue`, `assertThrows` 등 JUnit의 `Assertions`를 사용해
 * 성공/실패를 자동으로 판별하도록 작성해야 합니다.
 */

/*
 * ✅ [T]imely (적시에)
 * - 정의: TDD의 핵심. 테스트는 실제 코드가 작성되기 '직전'(Red 단계)에 '적시에' 작성되어야 합니다.
 * - 속성: 실제 코드를 *먼저* 작성한 후 테스트를 나중에 작성하는 것은 TDD가 아니라,
 * '테스트-라스트(Test-Last)'이며, 이는 종종 불필요하게 복잡한 테스트를 만듭니다.
 * - 실천: Red -> Green -> Refactor 사이클을 준수합니다.
 */
public class TddPatternsAndAntiPatterns {
}
