## 📚 Chapter 04. 컬렉션 자료구조 - 섹션 4.6 자료구조 선택 가이드

### 🎬 도입 스토리

뉴런테크의 메인 아키텍처 설계 회의 시간입니다. 당신은 100만 명의 실시간 접속자 ID를 관리하고, 특정 사용자가 현재 접속 중인지 확인하는 기능을 설계해야 합니다. 회의실 화이트보드 앞에 선 당신은 망설임 없이 "리스트(List)에 저장하면 되지 않을까요?"라고 말합니다.

그 순간, 창가에 앉아 있던 수석 엔지니어가 차분하게 질문을 던집니다. "신입 사원님, 리스트에서 특정 ID를 찾으려면 컴퓨터는 0번부터 마지막 번호까지 하나씩 다 열어봐야 합니다. 만약 운이 나빠서 찾는 사람이 마지막에 있다면 백만 번의 연산이 필요하죠. 하지만 우리 서비스는 0.01초의 지연도 허용하지 않습니다. 이 상황에서도 리스트가 최선일까요?"

당신은 뒤통수를 한 대 맞은 기분이 듭니다. '아차, 데이터가 많아질 때의 성능을 생각하지 못했구나!' 시니어 개발자는 단순히 문법을 아는 사람이 아닙니다. 수많은 도구 가방에서 **"지금 이 상황에 가장 빠르고 효율적인 도구"** 를 꺼낼 줄 아는 사람입니다. 오늘 우리는 지금까지 배운 리스트, 튜플, 딕셔너리, 세트를 한눈에 비교하고, 실무에서 어떤 기준으로 자료구조를 선택해야 하는지 그 '결정적 가이드'를 배울 것입니다.

---

### 자료구조 선택의 4대 핵심 기준

실무에서 자료구조를 선택할 때는 다음 네 가지 질문을 스스로에게 던져야 합니다.

1. **순서가 중요한가?** (Ordering)
2. **중복을 허용하는가?** (Duplicates)
3. **데이터를 수정해야 하는가?** (Mutability)
4. **검색 속도가 중요한가?** (Performance)

#### 자료구조별 특징 비교표

| 자료구조           | 기호    | 순서 여부  | 중복 여부     | 변경 가능  | 핵심 용도                                 |
| ------------------ | ------- | ---------- | ------------- | ---------- | ----------------------------------------- |
| **리스트(List)**   | `[]`    | **있음**   | 허용          | 가능       | 데이터의 나열, 추가/삭제가 잦은 경우      |
| **튜플(Tuple)**    | `()`    | **있음**   | 허용          | **불가능** | 고정된 값, 함수의 리턴값, 메모리 절약     |
| **딕셔너리(Dict)** | `{K:V}` | 있음(3.7+) | 키 중복 불가  | 가능       | 키를 통한 빠른 검색, 매핑 정보 관리       |
| **세트(Set)**      | `{}`    | 없음       | **중복 불가** | 가능       | 중복 제거, 집합 연산, 고속 존재 여부 확인 |

### 내부 동작 원리와 성능 (표기법 맛보기)

**1. 선형 탐색(Linear Search) vs 해시 탐색(Hash Lookup)**

- **리스트/튜플**:
  - 메모리 상에 데이터가 기차 칸처럼 연결되어 있습니다.
  - 특정 값을 찾으려면 첫 칸부터 끝 칸까지 순차적으로 확인해야 합니다.
  - 데이터가 개일 때 최대 번 확인하므로, 이를 **$O(n)$** 의 시간 복잡도라고 부릅니다.
  - 데이터가 많아질수록 정직하게 느려집니다.
- **딕셔너리/세트**:
  - 4.3, 4.4 섹션에서 배운 '해시 테이블'을 사용합니다.
  - 값을 입력하면 즉시 주소가 계산되어 나옵니다.
  - 데이터가 1개든 10억 개든 상관없이 한 번에 위치를 찾아내므로 **$O(1)$** 의 시간 복잡도를 가집니다.

**2. 메모리 사용량과 캐시 효율성**

- **튜플**은 생성 시 딱 필요한 만큼의 메모리만 OS에 요청합니다. 따라서 리스트보다 가볍고 빠릅니다.
- **딕셔너리와 세트**는 빠른 검색 속도를 위해 실제 데이터보다 훨씬 큰 메모리 공간을 미리 확보해 둡니다(해시 충돌 방지용). <br>즉, **"메모리를 더 써서 속도를 사는 방식"** 입니다.

**3. OS 레벨의 데이터 보호**

- **튜플**을 사용하면 OS가 해당 메모리 영역을 '읽기 전용'에 가깝게 취급할 수 있는 힌트를 얻습니다. <br> 멀티 스레드 환경이나 복잡한 시스템에서 데이터가 예기치 않게 변하는 사고를 원천 봉쇄합니다.

#### 시니어의 선택 가이드 (Decision Tree)

1. **고유한 값**들만 모으고 싶거나, **존재 여부**를 아주 빠르게 확인해야 하는가? ➔ **세트(Set)**
2. 데이터에 **이름(키)**을 붙여서 관리하고 싶은가? ➔ **딕셔너리(Dict)**
3. 데이터가 **절대 변하면 안 되거나**, 메모리를 극한으로 아껴야 하는가? ➔ **튜플(Tuple)**
4. 데이터의 **순서**가 중요하고, 수시로 **넣고 빼야** 하는가? ➔ **리스트(List)**

---

### 🎓 핵심 요약

이번 섹션에서 우리는 다음 내용을 배웠습니다:

1. **자료구조 선택의 기준**: 순서, 중복, 가변성, 성능이라는 4가지 관점에서 데이터를 바라보는 법을 익혔습니다.
2. **시간 복잡도의 실체**: 리스트는 데이터를 하나씩 찾는 , 세트와 딕셔너리는 즉시 찾아가는 $O(1)$의 성능을 가집니다.
3. **성능과 자원의 트레이드오프**: 빠른 속도를 위해 메모리를 더 사용하는 해시 기반 자료구조의 특징을 이해했습니다.
4. **실무형 설계**: 하나의 시스템에서 여러 자료구조를 유기적으로 결합하여 안정성과 속도를 동시에 잡는 아키텍처를 경험했습니다.
