"""
Section Overview
──────────────────────────────────────────

해결하고자 하는 문제:
대부분의 입문자는 print()를 "화면에 글자를 띄우는 마법"으로 생각합니다.
하지만 실제로는 OS의 표준 출력 스트림에 데이터를 전송하는 복잡한 과정입니다.
이 메커니즘을 이해해야 나중에 파일 저장, 네트워크 전송 등을 쉽게 배울 수 있습니다.

학습 목표:
- ✅ print() 함수의 핵심 매개변수 (sep, end, flush) 마스터
- ✅ 문자열 리터럴의 메모리 할당 이해
- ✅ 이스케이프 시퀀스(\n, \t)의 원리 파악
"""

# 기본 출력
print("Hello, World!")

# 다중 인자와 sep 구분자
print("Apple", "Banana", "Cherry", sep="🔥🔥")

# end 매개변수 줄바꿈 제어
print("Hello, ", end="...")
print("World")

# f-String
name = "Alice"
age = 30
print("My name is {name} and I am {age} years old.")

# anti-pattern:
result = print("i am output") # print()의 반환값은 None
print(f"The result of print() is: {result}")

# 이스케이프 시퀀스
print("Line 1 \n Line 2 \t \n backslash \\")

import time

print("Checking system", end = "", flush=True) # 버퍼를 채우기까지 기다렸다가 동작한다. \n이 없으면 버퍼가 가득 찰 때까지 출력하지 않는다.

for _ in range(3):
  time.sleep(0.5)
  print(".", end="", flush=True) # flush=True 옵션을 주면 버퍼를 비우고 바로 출력한다.