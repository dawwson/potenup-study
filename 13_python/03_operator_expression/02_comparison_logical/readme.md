## 📚 Chapter 03. 연산자와 표현식 - 섹션 3.2 비교 연산자와 논리 연산자

### 🎬 도입 스토리

뉴런테크의 보안 시스템 개발팀으로 자리를 옮긴 당신. 오늘 여러분의 임무는 '데이터 센터 출입 통제 엔진'의 핵심 로직을 설계하는 것입니다. 이 시스템은 단순히 문을 열고 닫는 것이 아닙니다.

"출입 카드의 ID가 등록된 명단에 있는가?"
"사용자의 나이가 만 18세 이상인가?"
"현재 시각이 허용된 업무 시간 안인가, 아니면 관리자 권한을 가졌는가?"

이런 질문들은 결국 **"예(True)"** 또는 **"아니오(False)"** 라는 두 가지 대답으로 귀결됩니다. 초보 개발자는 `if`문을 먼저 떠올리겠지만, 시니어 개발자는 그 이전에 실행되는 **비교 연산**과 **논리 연산**의 결과값이 메모리에서 어떻게 `bool` 객체로 생성되고, `CPU가 이를 얼마나 효율적으로 판단하는지에 집중`합니다. 특히 수만 개의 조건을 동시에 체크해야 하는 대규모 시스템에서 '단락 평가'라는 기법이 어떻게 시스템의 부하를 줄여주는지 이해하는 것이 오늘의 핵심입니다.

---

### 비교 연산자 (Comparison Operators)

비교 연산자는 두 값을 비교하여 그 결과를 **불리언(bool)** 타입인 `True` 또는 `False`로 반환합니다.

| 연산자 | 의미               | 예시 (a=10, b=20) | 결과    |
| ------ | ------------------ | ----------------- | ------- |
| `==`   | 같다               | `a == b`          | `False` |
| `!=`   | 같지 않다          | `a != b`          | `True`  |
| `>`    | 크다 (초과)        | `a > b`           | `False` |
| `<`    | 작다 (미만)        | `a < b`           | `True`  |
| `>=`   | 크거나 같다 (이상) | `a >= 10`         | `True`  |
| `<=`   | 작거나 같다 (이하) | `a <= b`          | `True`  |

### 논리 연산자 (Logical Operators)

논리 연산자는 여러 개의 불리언 값을 조합하여 하나의 결과를 만들어냅니다.

1. **`and` (논리곱)**: 모든 조건이 `True`일 때만 `True`를 반환합니다. ($A∧B$)
2. **`or` (논리합)**: 조건 중 하나라도 `True`이면 `True`를 반환합니다. ($A∨B$)
3. **`not` (논리부정)**: `True`는 `False`로, `False`는 `True`로 뒤집습니다. ($¬A$)

### 내부 동작 원리

**1. 불리언의 메모리 실체 (Integer Subclass)** <br>
파이썬에서 `bool`은 `int`의 자식 클래스입니다. 메모리 내부적으로 `True`는 정수 `1`, `False`는 정수 `0`으로 처리됩니다.

- **OS/메모리 관점**: <br> 아주 작은 데이터임에도 불구하고 파이썬은 이를 객체로 관리합니다. 앞서 배운 `None`과 마찬가지로 `True`와 `False` 역시 **싱글톤(Singleton)** 객체입니다. 즉, 프로그램 전체에서 단 하나의 `True` 객체와 단 하나의 `False` 객체만 존재하며, 모든 비교 결과는 이 두 주소 중 하나를 가리키게 됩니다.

**2. 단락 평가 (Short-circuit Evaluation)** <br>
논리 연산 시 파이썬 인터프리터는 매우 영리하게 동작합니다.

- **`and` 연산**:
  - 앞의 조건이 `False`라면, 뒤의 조건이 무엇이든 결과는 무조건 `False`입니다. <br> 이때 파이썬은 **뒤의 코드를 아예 실행하지 않고** 즉시 결과를 반환합니다.
- **`or` 연산**:
  - 앞의 조건이 `True`라면, 뒤의 조건과 상관없이 무조건 `True`입니다. <br>역시 뒤의 코드는 무시됩니다.
- **성능 이점**:
  - 이 메커니즘은 불필요한 연산을 줄여 CPU 자원을 절약합니다.
  - 예를 들어 `무거운_계산_함수() and False` 보다 `False and 무거운_계산_함_수()`가 훨씬 빠릅니다.

**3. `==` (값 비교) vs `is` (객체 비교)**

- `==`: 두 객체가 가진 **데이터(값)** 가 같은지 확인합니다.
- `is`: 두 변수가 메모리 상의 **동일한 주소(객체)** 를 가리키는지 확인합니다.
- 실무에서는 값을 비교할 때는 `==`를, `None`이나 `bool` 상수를 체크할 때는 `is`를 사용하는 것이 성능과 관례 면에서 권장됩니다.

### 주의사항 및 베스트 프랙티스

- **연산자 우선순위**:
  - `not` > `and` > `or` 순서로 계산됩니다.
  - 복잡한 논리식은 반드시 **괄호 `()`** 를 사용하여 의도를 명확히 하세요.
- **가독성**:
  - `(age >= 18) and (age < 65)` 처럼 괄호를 적절히 섞어주면 동료 개발자가 코드를 읽는 속도가 비약적으로 향상됩니다.
- **체이닝(Chaining)**:
  - 파이썬은 `10 < age < 20`과 같은 수학적 표기법을 지원합니다.
  - 이는 `(10 < age) and (age < 20)`과 동일하게 동작하며 파이썬스러운(Pythonic) 코드의 전형입니다.

### 🎓 핵심 요약

1. **비교 연산자**:
   - `==`, `!=`, `<`, `>` 등을 통해 데이터 간의 관계를 파악하고 `bool` 결과를 얻는 법을 익혔습니다.
2. **논리 연산자**:
   - `and`, `or`, `not`을 사용하여 여러 조건을 논리적으로 결합하는 법을 배웠습니다.
3. **단락 평가(Short-circuit)**:
   - 파이썬이 논리 연산 시 불필요한 계산을 건너뛰어 성능을 최적화하는 원리를 이해했습니다.
4. **메모리 싱글톤**:
   - `True`와 `False`는 메모리상에 단 하나만 존재하는 객체이며, `is` 연산자로 이를 확인할 수 있음을 배웠습니다.
