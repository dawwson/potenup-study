package com.ogriffers.chap02.section02;

/**
 * 제네릭스란
 * - 클래스나 메섣에 사용할 타입을 나중에 지정할 수 있도록 해주는 기능이다.
 * - 타입 파라미터(T)를 사용해서 객체를 만들 대 타입을 정한다.
 * - 비유 : 상자 (box)라 만들 때, 안에 넣을 물건(타입)을 나중에 정하는 것이다.
 *
 * 제네릭스가 필요한 이유
 * - 제네릭스 없이 상자를 만드면 모든 물건(object)를 넣을 수 있지만, 꺼낼 때마다 형변환이 필요하다.
 *   -> 꺼낼 때
 * -
 */

public class Application01 {
    public static void main(String[] args) {
        System.out.println("Hello");
    }
}
